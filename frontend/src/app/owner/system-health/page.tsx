import OwnerSectionPlaceholder from '../OwnerSectionPlaceholder';

export default function OwnerSystemHealthPage() {
  return (
    <OwnerSectionPlaceholder
      title="System Health"
      description="Chequeos de salud para auth, RAG, generación e indexación (solo lectura en esta fase)."
    />
  );
}
